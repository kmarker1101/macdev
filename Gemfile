# frozen_string_literal: true

source 'https://rubygems.org'

# TOML parsing for manifest files
gem 'toml-rb', '~> 3.0'

group :development, :test do
  gem 'rspec', '~> 3.13'
  gem 'rubocop', require: false
end
